# Example output from g.calibrate

data.calibrate is example output from
[g.calibrate](https://wadpac.github.io/GGIR/reference/g.calibrate.md)

## Usage

``` r
data(data.calibrate)
```

## Format

The format is: chr "data.calibrate"

## Source

The data was collected on one individual for testing purposes

## Examples

``` r
data(data.calibrate)
```
